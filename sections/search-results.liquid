<section class="page-width section">
  <div class="section-head">
    <div>
      <h1>Search</h1>
      {%- if search.performed -%}
        {%- if search.results_count == 0 -%}
          <p>No results for "{{ search.terms | escape }}"</p>
        {%- else -%}
          <p>{{ search.results_count }} results for "{{ search.terms | escape }}"</p>
        {%- endif -%}
      {%- endif -%}
    </div>
  </div>

  <form action="{{ routes.search_url }}" method="get" role="search" class="search-modal__form" style="margin-bottom:2rem;">
    <input type="hidden" name="type" value="product">
    <input class="search-modal__input" type="search" name="q" value="{{ search.terms | escape }}" placeholder="Search cards, sets, sealed...">
    <button type="submit" class="button">Search</button>
  </form>

  {%- if search.performed -%}
    <div class="product-grid">
      {%- paginate search.results by 24 -%}
        {%- for item in search.results -%}
          {%- if item.object_type == 'product' -%}
            {%- liquid
              assign product = item
              assign image_alt = product.featured_image.alt | default: product.title
            -%}
            <a class="card" href="{{ product.url }}">
              <div class="card__media">
                {%- if product.available == false -%}
                  <span class="card__badge card__badge--sold">Sold out</span>
                {%- endif -%}
                {%- if product.featured_image != blank -%}
                  <img
                    src="{{ product.featured_image | image_url: width: 480 }}"
                    srcset="
                      {{ product.featured_image | image_url: width: 240 }} 240w,
                      {{ product.featured_image | image_url: width: 360 }} 360w,
                      {{ product.featured_image | image_url: width: 480 }} 480w,
                      {{ product.featured_image | image_url: width: 700 }} 700w
                    "
                    sizes="(max-width: 749px) 45vw, 23vw"
                    alt="{{ image_alt | escape }}"
                    width="{{ product.featured_image.width }}"
                    height="{{ product.featured_image.height }}"
                    loading="lazy"
                  >
                {%- else -%}
                  {{ 'product-1' | placeholder_svg_tag }}
                {%- endif -%}
              </div>
              <div class="card__body">
                <div class="card__title">{{ product.title | escape }}</div>
                <div class="card__footer">
                  <span class="card__price">{{ product.price | money }}</span>
                </div>
              </div>
            </a>
          {%- endif -%}
        {%- endfor -%}

        {%- if paginate.pages > 1 -%}
          <div class="pagination" style="grid-column:1/-1">
            {{ paginate | default_pagination }}
          </div>
        {%- endif -%}
      {%- endpaginate -%}
    </div>
  {%- endif -%}
</section>

{% schema %}
{
  "name": "Search results",
  "tag": "section",
  "class": "section-search-results",
  "settings": []
}
{% endschema %}
