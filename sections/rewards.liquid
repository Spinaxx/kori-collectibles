{%- include 'loyalty-auth-urls' -%}

{%- liquid
  assign balance = 0

  if customer
    assign points_key = settings.loyalty_points_metafield | default: 'custom.loyalty_points' | strip
    assign points_parts = points_key | split: '.'
    assign points_ns = points_parts[0] | strip
    assign points_id = points_parts[1] | strip

    if points_ns != blank and points_id != blank
      assign balance = customer.metafields[points_ns][points_id].value | default: 0 | plus: 0
    endif

    if balance == 0
      assign balance = customer.metafields.custom.loyalty_points.value | default: 0 | plus: 0
    endif
  endif

  assign points_rate = section.settings.points_per_gbp | default: settings.loyalty_points_per_gbp | default: 1
  assign redeem_points = section.settings.redeem_points | default: settings.loyalty_redeem_points | default: 100
  assign redeem_value = section.settings.redeem_value_gbp | default: settings.loyalty_redeem_value_gbp | default: 5
  assign redeem_blocks = balance | divided_by: redeem_points | floor
  assign redeemable_gbp = redeem_blocks | times: redeem_value

  assign active_redeem_code = blank
  assign redeem_code_key = settings.loyalty_redeem_code_metafield | default: 'custom.loyalty_redeem_code' | strip
  assign redeem_code_parts = redeem_code_key | split: '.'
  assign redeem_code_ns = redeem_code_parts[0] | strip
  assign redeem_code_id = redeem_code_parts[1] | strip

  if customer
    if redeem_code_ns != blank and redeem_code_id != blank
      assign active_redeem_code = customer.metafields[redeem_code_ns][redeem_code_id].value
    endif
    if active_redeem_code == blank
      assign active_redeem_code = customer.metafields.custom.loyalty_redeem_code.value
    endif
    assign active_redeem_code = active_redeem_code | strip
  endif
-%}

<section class="section rewards">
  <div class="page-width">
    <div class="rewards__hero">
      <p class="rewards__eyebrow">{{ section.settings.eyebrow | escape }}</p>
      <h1 class="rewards__title">{{ section.settings.heading | default: page.title | escape }}</h1>
      {%- if section.settings.intro != blank -%}
        <p class="rewards__intro">{{ section.settings.intro | escape }}</p>
      {%- endif -%}
    </div>

    {%- if customer -%}
      <div class="rewards__balance-card">
        <div class="rewards__balance-main">
          <span class="rewards__balance-label">Your balance</span>
          <p class="rewards__balance-value">
            <span data-loyalty-balance>{{ balance | default: 0 }}</span>
            <span class="rewards__balance-unit">points</span>
          </p>
          {%- if redeemable_gbp > 0 -%}
            <p class="rewards__balance-redeem">
              Worth up to {{ redeemable_gbp | times: 100 | money }} off your next order
            </p>
          {%- else -%}
            <p class="rewards__balance-redeem">
              {{ redeem_points | minus: balance }} more points until your first £{{ redeem_value }} reward
            </p>
          {%- endif -%}
        </div>
        <div class="rewards__balance-actions">
          <a class="button" href="{{ routes.all_products_collection_url }}">Shop to earn</a>
        </div>
      </div>

      <div class="rewards__redeem-panel">
        <h2 class="rewards__redeem-title">Redeem your points</h2>
        {%- capture redeem_form_blocks -%}
          {%- for block in section.blocks -%}
            {%- if block.type == '@app' -%}
              {%- render block -%}
            {%- endif -%}
          {%- endfor -%}
        {%- endcapture -%}
        {%- render 'loyalty-redeem-card', balance: balance, form_content: redeem_form_blocks, show_active_code: false, active_redeem_code: active_redeem_code -%}
      </div>

      {%- render 'loyalty-active-codes', redeem_value: redeem_value, active_redeem_code: active_redeem_code -%}
    {%- else -%}
      <div class="rewards__balance-card rewards__balance-card--guest">
        <div class="rewards__balance-main">
          <span class="rewards__balance-label">Shop account required</span>
          <p class="rewards__balance-value rewards__balance-value--guest">Earn on every order</p>
          <p class="rewards__balance-redeem">
            Sign in or create a free account to collect points automatically after each purchase.
          </p>
        </div>
        <div class="rewards__balance-actions">
          <a class="button" href="{{ loyalty_sign_in_url }}">Sign in</a>
          <a class="button button--secondary" href="{{ loyalty_register_url }}">Create account</a>
        </div>
      </div>
    {%- endif -%}

    <div class="rewards__grid">
      <div class="rewards__panel">
        <h2>How you earn</h2>
        <ul class="rewards__list" role="list">
          <li>
            <span class="rewards__list-num">01</span>
            <div>
              <strong>{{ points_rate }} point{% if points_rate != 1 %}s{% endif %} per £1 spent</strong>
              <p>Points are added to your account after your order is paid.</p>
            </div>
          </li>
          {%- for block in section.blocks -%}
            {%- if block.type == 'earn_rule' -%}
              <li {{ block.shopify_attributes }}>
                <span class="rewards__list-num">{{ block.settings.number | escape }}</span>
                <div>
                  <strong>{{ block.settings.heading | escape }}</strong>
                  <p>{{ block.settings.text | escape }}</p>
                </div>
              </li>
            {%- endif -%}
          {%- endfor -%}
        </ul>
      </div>

      <div class="rewards__panel">
        <h2>How you redeem</h2>
        <ul class="rewards__list" role="list">
          <li>
            <span class="rewards__list-num">01</span>
            <div>
              <strong>{{ redeem_points }} points = £{{ redeem_value }} off</strong>
              <p>Redeem instantly on the rewards page when you have enough points.</p>
            </div>
          </li>
          {%- for block in section.blocks -%}
            {%- if block.type == 'redeem_rule' -%}
              <li {{ block.shopify_attributes }}>
                <span class="rewards__list-num">{{ block.settings.number | escape }}</span>
                <div>
                  <strong>{{ block.settings.heading | escape }}</strong>
                  <p>{{ block.settings.text | escape }}</p>
                </div>
              </li>
            {%- endif -%}
          {%- endfor -%}
        </ul>
      </div>
    </div>

    {%- if section.settings.fine_print != blank -%}
      <div class="rewards__fine-print">{{ section.settings.fine_print }}</div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Rewards",
  "tag": "section",
  "class": "section-rewards",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Kori Collectors Club"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Rewards points"
    },
    {
      "type": "textarea",
      "id": "intro",
      "label": "Introduction",
      "default": "Collect points every time you shop. The more you hunt, the more you save."
    },
    {
      "type": "range",
      "id": "points_per_gbp",
      "min": 1,
      "max": 20,
      "step": 1,
      "label": "Points earned per £1 spent",
      "default": 1
    },
    {
      "type": "number",
      "id": "redeem_points",
      "label": "Points needed per reward",
      "default": 100
    },
    {
      "type": "number",
      "id": "redeem_value_gbp",
      "label": "Reward value (GBP)",
      "default": 5
    },
    {
      "type": "richtext",
      "id": "fine_print",
      "label": "Fine print",
      "default": "<p>Points have no cash value. Rewards apply to eligible products only and cannot be combined with some promotions.</p>"
    }
  ],
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "earn_rule",
      "name": "Earn rule",
      "settings": [
        { "type": "text", "id": "number", "label": "Number", "default": "02" },
        { "type": "text", "id": "heading", "label": "Heading", "default": "Use your shop account" },
        { "type": "textarea", "id": "text", "label": "Text", "default": "Checkout while signed in so points land on the right account." }
      ]
    },
    {
      "type": "redeem_rule",
      "name": "Redeem rule",
      "settings": [
        { "type": "text", "id": "number", "label": "Number", "default": "02" },
        { "type": "text", "id": "heading", "label": "Heading", "default": "At checkout" },
        { "type": "textarea", "id": "text", "label": "Text", "default": "Email us from the rewards panel to claim your discount code." }
      ]
    }
  ],
  "presets": [
    {
      "name": "Rewards",
      "blocks": [
        {
          "type": "earn_rule",
          "settings": {
            "number": "02",
            "heading": "Use your shop account",
            "text": "Checkout while signed in so points land on the right account."
          }
        },
        {
          "type": "redeem_rule",
          "settings": {
            "number": "02",
            "heading": "Redeem",
            "text": "Open the rewards panel and email us when you have enough points."
          }
        }
      ]
    }
  ]
}
{% endschema %}
